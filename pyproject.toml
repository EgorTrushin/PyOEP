[tool.ruff]
line-length = 120
target-version = "py312"

[tool.ruff.lint]
select = ["E", "W", "F", "I", "UP", "B", "C4", "SIM", "NPY", "RUF"]

[tool.ruff.lint.isort]
known-first-party = ["methods", "utils"]

[tool.ruff.format]
docstring-code-format = true
